Crude testing experiment with jsdom

usage:

* install node.js (http://nodejs.org)
* install npm (http://github.com/isaacs/npm)
* install jsdom (npm install jsdom)
* install HTMLParser (npm install htmlparser)
* install colors (npm install colors)

to run: node test.js